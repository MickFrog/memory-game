import React from "react";
import './HowTo.css'

const HowTo = () => {
    return (
        <div className="instr">
            Select a card from the pack and make sure to select it only once.
        </div>
    )
}

export default HowTo;