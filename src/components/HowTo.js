import React from "react";
import './HowTo.css'

const HowTo = () => {
    return (
        <div className="instr">
            Select a card from each pack and make sure to select it only once in the game!
        </div>
    )
}

export default HowTo;